---

  - hosts: tag_Name_control_{{ lookup('env', 'DEPLOY') }}

    tasks:

      - name: stop swarm-manager
        service:
          name="swarm-manager"
          state="stopped"

      - name: enable and start swarm-manager
        service:
          name="swarm-manager"
          state="started"
          enabled="yes"
