---

    config: >
      rs.initiate(
        {
          _id: "sugarush-alpha-config",
          configsvr: true,
          members: [
            { _id : 0, host : "database-1:27019" },
            { _id : 1, host : "database-2:27019" },
            { _id : 2, host : "database-3:27019" }
          ]
        }
      )

    shard: >
      rs.initiate(
        {
          _id: "sugarush-alpha-shard",
          members: [
            { _id : 0, host : "database-1:27018" },
            { _id : 1, host : "database-2:27018" },
            { _id : 2, host : "database-3:27018" }
          ]
        }
      )

    - hosts: tag_Name_control_{{ lookup('env', 'DEPLOY') }}

      limit: 1
