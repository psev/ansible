---

  - name: install abs
    command: pacman --noconfirm -Sy abs

  - name: create build user
    user:
      name="build"
      createhome="no"
      home="/opt/build"

  - name: create /opt/build
    file:
      path="/opt/build"
      mode="0755"
      owner="build"
      state="directory"
